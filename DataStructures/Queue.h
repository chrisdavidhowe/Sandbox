//
// Created by Chris Howe on 8/5/20.
//

#pragma once

/* Queue's are used when things don’t have to be processed immediately,
 * but have to be processed in First In First Out (FIFO) order like Breadth First Search.
*/

/*
class Queue
{
public:
    Queue(int capacity);
    ~Queue();
    int  getFront();
    int  getRear();
    void enqueue(int value);
    void dequeue();
    void print();

private:
    int* data_;
    int  length_   { 0 };
    int  capacity_ { 10 };
};
*/